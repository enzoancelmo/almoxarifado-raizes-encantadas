import { Injectable } from '@angular/core';import { HttpClient } from '@angular/common/http';import { Observable } from 'rxjs';import { environment } from '../../../environments/environment';import { SmartSuggestion } from '../models/suggestion.model';@Injectable({providedIn:'root'})export class SmartSuggestionService{constructor(private readonly http:HttpClient){}getSuggestions():Observable<SmartSuggestion[]>{return this.http.get<SmartSuggestion[]>(environment.apiUrl+'/smart-suggestions');}}
