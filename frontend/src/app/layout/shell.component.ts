import { Component } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router,RouterLink,RouterLinkActive,RouterOutlet } from '@angular/router';import { AuthService } from '../core/services/auth.service';@Component({selector:'app-shell',standalone:true,imports:[FormsModule,RouterLink,RouterLinkActive,RouterOutlet],templateUrl:'./shell.component.html',styleUrl:'./shell.component.css'})export class ShellComponent{menuOpen=false;searchTerm='';readonly user=this.auth.currentUser;constructor(private readonly auth:AuthService,private readonly router:Router){}searchProducts():void{const query=this.searchTerm.trim();void this.router.navigate(['/produtos'],{queryParams:query?{q:query}:{}});}logout():void{this.auth.logout();void this.router.navigate(['/login']);}}
